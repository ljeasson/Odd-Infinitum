/// increase_number_to(start, stop, num_speed)
// Increase a number from a starting number to an ending number

start = argument0;
stop = argument1;
num_speed = argument2;

if (start != stop)
    start += num_speed;
